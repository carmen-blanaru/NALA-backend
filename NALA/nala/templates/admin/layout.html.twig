
{% extends "base.html.twig" %}


{% block body %}

<!--  this part will be on the all pages of the backoffice, on the left side, it behaves like a navigation bar-->
<div class="container">
		<div class="row">
			<aside class="col-sm-3 mt-4">
				<h2>Dashboard</h2>
				<div class="list-group">
				{% if app.user%}
					<a href="{{path ('admin_dashboard')}}" class="list-group-item list-group-item-action" aria-current="true">Accueil</a>
					<a href="#" class="list-group-item list-group-item-action">Publications</a>
					<a href="{{path ('admin_comment_list')}}" class="list-group-item list-group-item-action">Commentaires</a>
					<a href="{{path ('admin_user_list')}}" class="list-group-item list-group-item-action">Comptes utilisateurs</a>
					{% else %}
					<p class='mt-3 list-group-item-info text-center rounded-2 px-1'> Veuillez vous connecter pour avoir accès au menu! </p>
					{% endif %}
				</div>
			</aside>

<!--only this part will be changing on click-->
			<main class="col-sm-9 card">
				{% block content %}{% endblock %}
			</main>
		</div>
	</div>

{% endblock %}